# Linux 系统初始化器

[English](../README.md) | 简体中文

一个现代化的终端用户界面（TUI）应用程序，用于全面的 Linux 系统初始化和配置。基于 Python Rich/Textual 框架构建。

## 🌟 特性

- **现代 TUI 界面**：基于 Python Rich/Textual 构建，提供美观、响应式的终端界面
- **键盘优先操作**：完整的键盘导航支持，包括 hjkl/方向键、Tab 切换焦点和数字快捷键
- **9 个完整模块**：系统信息、包管理器、应用安装器、Homebrew、Vim、Zsh/Tmux、Claude/Codex、用户管理和设置
- **配置驱动**：基于 YAML 的配置系统，支持模块化设置
- **异步操作**：非阻塞 UI，支持异步任务管理和进度跟踪
- **丰富的模态系统**：19+ 个模态对话框，用于确认、进度和用户交互
- **跨平台**：支持多种 Linux 发行版，自动检测
- **无头服务器支持**：针对 SSH 和无头环境进行优化
- **主题系统**：3 个内置主题（默认、暗色、亮色）
- **全面的日志记录**：模块级和文件级日志配置
- **自动终端清理**：防止异常退出后的终端状态问题

## 🚀 快速开始

### 前置要求

- Python 3.8 或更高版本
- Linux 操作系统（Ubuntu/Debian/Arch/Fedora 等）
- 支持 256 色的终端

### 安装

1. **克隆仓库：**

```bash
git clone https://github.com/DeltaV235/Initializer.git
cd Initializer
```

2. **运行安装脚本：**

```bash
./install.sh

# 安装开发依赖
./install.sh --dev

# 自动确认模式（非交互式）
./install.sh --auto-confirm
```

3. **启动应用程序：**

```bash
./run.sh
```

**或手动启动：**

```bash
source .venv/bin/activate
python main.py
```

### 命令行选项

```bash
python main.py --help

选项：
  -c, --config-dir TEXT   配置目录路径
  --headless              以无头模式运行（无动画）
  --debug                 启用调试模式和详细日志
  --help                  显示帮助信息并退出
```

## ⌨️ 键盘导航

应用程序采用**键盘优先操作**设计：

### 全局快捷键
- **q**：退出应用程序
- **Tab**：在面板之间切换焦点
- **h/l**：导航到左/右面板（Vim 风格）
- **j/k**：在焦点面板内上/下移动
- **Enter**：选择/激活当前项目
- **1-9**：按数字快速跳转到段
- **s**：跳转到设置

### 视觉反馈
- **蓝色边框**：指示当前焦点所在的活动面板
- **箭头（▶）**：显示当前选中的段
- **帮助框**：底部区域显示可用的快捷键

## 🔗 远程同步和执行

对于非 WSL/macOS/Windows 环境，可同步并在远程 Linux 服务器上执行：

```bash
# 同步到默认远程服务器（user@192.168.1.100:~/Initializer）
tools/sync-to-remote.sh

# 预览更改的试运行
tools/sync-to-remote.sh -n

# 自定义主机/用户/目标
tools/sync-to-remote.sh -H 192.168.1.100 -u user -D ~/Initializer
```

然后在远程服务器上：

```bash
ssh user@192.168.1.100
cd ~/Initializer
./install.sh
./run.sh
```

**注意**：WSL 用户可以本地测试，但应在实际 Linux 环境中验证。

## 📋 模块

全部 9 个模块均已**完整实现并可用于生产环境**：

### 1. 系统状态

- 实时系统信息显示
- 硬件详情（CPU、内存、磁盘、网络）
- 软件信息（操作系统、内核、包管理器）
- 系统资源监控

### 2. 包管理器

- 自动检测可用的包管理器（apt、yum、pacman 等）
- 配置镜像源以加快下载速度
- 更新包管理器配置
- 支持多个发行版

### 3. 应用管理器

- 批量应用安装
- 预配置的应用列表（apt/Homebrew）
- 安装进度跟踪
- 依赖管理
- 快速验证和双层检查

### 4. Homebrew 管理

- Homebrew for Linux 安装
- 镜像源配置
- 基础包安装
- 版本管理

### 5. Vim 管理

- NeoVim 安装（最低版本 0.11.2）
- LazyVim 配置设置
- 插件管理
- 安装进度跟踪

### 6. Tmux & ZSH 管理器

- Zsh shell 安装和配置
- Oh-my-zsh 框架设置
- Tmux 安装和配置
- Shell 迁移和配置备份
- 自定义提示符和主题设置

### 7. Claude & Codex 管理

- Claude Code CLI 安装
- Codex CLI 安装
- 安装方法检测（brew/手动）
- 卸载信息模态框
- 版本管理

### 8. 用户管理

- 用户创建和配置
- SSH 密钥设置和管理
- 用户权限管理
- Sudo 配置

### 9. 设置

- 应用程序首选项
- 主题切换（默认/暗色/亮色）
- 模块启用/禁用
- 配置管理

## ⚙️ 配置

配置文件位于 `config/` 目录：

### 主要配置文件

- **`app.yaml`**：主应用程序设置、UI 首选项和行为
- **`modules.yaml`**：全部 9 个模块的模块特定配置
- **`themes.yaml`**：UI 主题（默认、暗色、亮色）和颜色方案
- **`logging.yaml`**：详细的日志配置（模块级和文件级）
- **`applications_apt.yaml`**：APT 包管理器应用列表
- **`applications_homebrew.yaml`**：Homebrew 应用列表

### 自定义配置

您可以修改配置文件来自定义应用程序行为。编辑 `config/` 目录中的 YAML 文件并重启应用程序。

## 🎨 主题

应用程序支持三个内置主题：

- **`default`**：标准蓝色主题，高对比度
- **`dark`**：暗色模式主题，适合低光环境
- **`light`**：亮色主题，适合明亮的终端

在设置模块中切换主题或编辑 `config/app.yaml`。

## 🖥️ 截图

### 系统状态
![系统状态](Screenshot-01.png)
*实时系统状态监控和信息显示*

### 应用管理器
![应用管理器](Screenshot-02.png)
*应用程序安装和管理界面*

### Vim 管理
![Vim 管理](Screenshot-03.png)
*Vim 配置和插件管理*

### Tmux & ZSH 管理器
![Tmux & ZSH 管理器](Screenshot-04.png)
*终端复用器和 Shell 配置界面*

## 🔧 开发

### 项目统计

- **Python 代码总量**：23,777 行
- **源文件**：62 个 Python 文件
- **UI 屏幕**：39 个屏幕/模态组件
- **模块**：12 个功能模块
- **配置文件**：6 个 YAML 配置文件
- **开发工具**：3 个 shell 脚本

### 项目结构

```text
Initializer/
├── config/                    # 配置文件
│   ├── app.yaml              # 主应用设置
│   ├── modules.yaml          # 模块配置
│   ├── themes.yaml           # UI 主题
│   ├── logging.yaml          # 日志配置
│   ├── applications_apt.yaml # APT 包
│   └── applications_homebrew.yaml # Homebrew 包
├── src/initializer/          # 主 Python 包
│   ├── app.py               # TUI 应用类
│   ├── config_manager.py    # 配置管理
│   ├── modules/             # 12 个功能模块
│   │   ├── system_info.py
│   │   ├── package_manager.py
│   │   ├── app_installer.py
│   │   ├── vim_manager.py
│   │   ├── zsh_manager.py
│   │   ├── claude_codex_manager.py
│   │   ├── sudo_manager.py
│   │   └── ...
│   ├── ui/                  # UI 组件
│   │   ├── screens/         # 39 个屏幕/模态文件
│   │   │   ├── main_menu.py
│   │   │   ├── main_menu_components/ # 12 个子组件
│   │   │   ├── settings.py
│   │   │   ├── vim_management.py
│   │   │   ├── zsh_manager.py
│   │   │   └── modals/      # 19+ 个模态对话框
│   │   └── styles.css       # Textual CSS 样式
│   └── utils/               # 工具函数
├── tools/                   # 开发工具
│   ├── sync-to-remote.sh   # 远程部署
│   ├── reset-terminal.sh   # 终端清理
│   └── check-test-environment.sh # 环境检测
├── legacy/                  # 原始 bash 脚本（备份）
├── install.sh              # 安装脚本
├── run.sh                  # 运行包装器（带清理）
├── uninstall.sh           # 卸载脚本
└── main.py                # 应用程序入口点
```

### 技术栈

**核心依赖：**
- **Rich** (>=13.0.0)：终端格式化和渲染
- **Textual** (>=0.41.0)：TUI 框架
- **PyYAML** (>=6.0.0)：配置文件处理
- **Click** (>=8.0.0)：命令行界面
- **psutil** (>=5.9.0)：系统信息收集
- **distro** (>=1.8.0)：Linux 发行版检测

**开发依赖：**
- **pytest** (>=7.0.0)：测试框架
- **black** (>=22.0.0)：代码格式化
- **flake8** (>=5.0.0)：代码检查
- **mypy** (>=1.0.0)：类型检查

### 开发命令

```bash
# 代码格式化
black src/

# 代码检查
flake8 src/

# 类型检查
mypy src/

# 测试
pytest

# 启用调试日志运行
./run.sh --debug
```

### 添加新模块

1. **在 `src/initializer/modules/` 中创建模块**
   ```python
   class MyModule:
       def __init__(self):
           pass

       def execute(self):
           # 实现
           pass
   ```

2. **添加配置到 `config/modules.yaml`**
   ```yaml
   my_module:
     enabled: true
     settings:
       option1: value1
   ```

3. **在 `src/initializer/ui/screens/` 中创建 UI 屏幕**
   ```python
   from textual.screen import Screen

   class MyModuleScreen(Screen):
       def compose(self):
           yield Header()
           yield MyContent()
           yield Footer()
   ```

4. **在主菜单中注册**（`src/initializer/ui/screens/main_menu.py`）
   ```python
   SEGMENTS = [
       # ... 现有段
       {"id": "my_module", "label": "My Module"},
   ]
   ```

### 架构模式

- **异步操作**：使用 `@work` 装饰器进行非阻塞任务
- **模态对话框**：一致的尺寸系统，包含 8 个 CSS 类
- **事件处理**：Textual 的基于消息的事件系统
- **响应式属性**：使用 `reactive()` 自动更新 UI
- **配置管理**：基于 YAML 的继承配置
- **日志记录**：结构化日志，支持模块级控制

## 🛠️ 工具和脚本

### 安装脚本（`install.sh`）

```bash
# 标准安装
./install.sh

# 安装开发依赖
./install.sh --dev

# 保留旧版脚本
./install.sh --legacy

# 自动确认所有提示
./install.sh --auto-confirm
```

### 运行脚本（`run.sh`）

自动处理：
- 虚拟环境激活
- Python 路径设置
- 退出后的终端状态清理

### 远程同步（`tools/sync-to-remote.sh`）

部署到远程 Linux 服务器进行测试：
- 支持自定义主机/用户/目标
- 试运行模式确保安全
- 排除不必要的文件（.git、.venv、__pycache__）

### 终端重置（`tools/reset-terminal.sh`）

修复终端状态问题：
- 退出备用屏幕缓冲区
- 禁用鼠标跟踪
- 显示光标
- 重置终端属性

## 📜 旧版脚本

原始 bash 脚本保留在 `legacy/` 目录中供参考：

```bash
cd legacy/01-linux-initial-scripts
./00-main.sh
```

这些脚本提供相同的功能，但没有现代 TUI 界面。

## 🐛 故障排除

### 终端显示问题

如果异常退出后终端显示不正确：

```bash
./tools/reset-terminal.sh
```

### 权限问题

确保脚本可执行：

```bash
chmod +x install.sh run.sh uninstall.sh
chmod +x tools/*.sh
```

### 虚拟环境问题

重新创建虚拟环境：

```bash
rm -rf .venv
./install.sh
```
